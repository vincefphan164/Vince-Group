# Bài học — cấp hệ thống (áp dụng MỌI project, không riêng Vince-Group)

> Đọc trước khi làm việc không quen thuộc (skill `hoc-hoi-tu-sai-lam`). Ghi NGAY khi sai — 1 dòng, không văn mẫu. Grep trước khi ghi, tránh trùng.

[[index]]

- 2026-09-03 · main · SAI: nghĩ Windows PowerShell 5.1 và PowerShell 7 (`pwsh`) là 1 → THẬT: 2 binary khác nhau (`powershell.exe` = 5.1 lõi hệ thống, `pwsh.exe` = 7 cài riêng qua winget), gọi nhầm `powershell` gây lỗi encoding UTF-8 (đọc sai literal tiếng Việt trong .ps1 không BOM) + lỗi `Add-Member` trên hashtable rỗng → TRÁNH: luôn gọi `pwsh`, không gọi `powershell`; nếu phải đặt literal có dấu trong .ps1, dùng placeholder ASCII thay vì ký tự Unicode
- 2026-09-03 · main · SAI: tưởng có thể sửa `-replace` trong PowerShell bằng cách đọc/ghi file qua `Get-Content`/`Set-Content` mặc định → THẬT: Windows PowerShell 5.1 đọc/ghi UTF-8-không-BOM sai bằng encoding mặc định → TRÁNH: dùng `[System.IO.File]::ReadAllText($p, [System.Text.Encoding]::UTF8)` / `WriteAllText` trực tiếp khi cần chắc chắn UTF-8
- 2026-09-03 · main · SAI: nghĩ `hivecontrol` CLI tự nhận diện repo đang đứng qua `cd` → THẬT: bị khoá cứng vào repo lúc session được DevSwarm spawn ra (biến môi trường `DEVSWARM_REPO_ID`, cố định suốt đời session) — `cd` sang thư mục repo khác KHÔNG đổi được → TRÁNH: muốn `hivecontrol workspace create` cho repo khác, phải mở project đó thật sự trong app DevSwarm (sinh session mới đúng `DEVSWARM_REPO_ID`), không gọi được từ session của repo khác
- 2026-09-03 · main · SAI: tưởng agent OpenCode tự đọc được file ở repo khác qua đường dẫn tuyệt đối vì permission block có `read: allow` → THẬT: đọc file NGOÀI thư mục project hiện tại là 1 permission RIÊNG (`external_directory`), mặc định `ask` — chạy headless (`opencode run`) không có ai duyệt nên tự động reject → TRÁNH: khi project cần đọc file ở repo khác (VD trỏ về luật Vince-Group), phải khai rõ trong `opencode.json` → `permission.external_directory: {"<path>\\*": "allow"}`, không chỉ dựa vào `read: allow`
- 2026-09-03 · main · SAI: đặt 4 file agent + `obsidian/` + `CLAUDE.md` mẫu TĨNH nằm sẵn trong `PROJECT-TEMPLATE/` (trùng tên với file thật ở gốc Vince-Group) → THẬT: 2 bản cùng tên trong 1 repo tự gây rối, dễ đọc nhầm bản mẫu là luật/bộ nhớ thật, và dễ lệch nhau theo thời gian → TRÁNH: template không đặt file trùng tên bản thật; dùng script sinh MỚI tại thời điểm cần (copy tươi từ nguồn duy nhất) hoặc đặt tên rõ ràng không đụng hàng (`*.template`)
- 2026-09-03 · main · SAI: coi việc "báo cáo tiến độ project X" là việc của phiên Vince-Group (tường thuật lại trong chat Vince-Group) → THẬT: mỗi project tự quản bộ nhớ + báo cáo của chính nó (`obsidian/log.md` riêng) — phiên Vince-Group chỉ brainstorm/bàn giao, KHÔNG chạy hộ/tường thuật hộ việc của project khác → TRÁNH: việc + báo cáo của project nào nằm trong chính repo đó, không proxy qua Vince-Group
